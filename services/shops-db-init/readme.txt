In this folder is a small helper function to get and load shops data into database.
Use it only if database delivered with repo is missing/corrupted.

How to use:
1. Navigate to folder containing init-shops.ts
2. npm ts-node init-shops

